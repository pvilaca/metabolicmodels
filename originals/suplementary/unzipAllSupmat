find . -name '*.zip' -exec sh -c 'unzip -o -d "${0%.*}" "$0"' '{}' ';'

find . -name '*.zip' -exec sh -c 'unzip -o -d "${0%/*}" "${0%}"' '{}' ';'

find . -name '*.xml' -exec sh -c 'cp --parents "${0%}" ../sbml/' '{}' ';'